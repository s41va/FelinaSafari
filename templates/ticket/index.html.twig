{# templates/ticket/confirmation.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Confirmación de Ticket{% endblock %}

{% block body %}
    <div class="container mt-5" style="max-width: 600px;">
        <div class="card shadow-lg border-0">
            <div class="card-header bg-success text-white text-center py-4">
                <h2 class="mb-0">🌿 ¡Compra realizada con éxito!</h2>
                <p class="mb-0 opacity-75">Tu entrada a la Reserva Natural ha sido confirmada.</p>
            </div>

            <div class="card-body p-4">
                <div class="text-center mb-4">
                    <p class="text-muted small mb-1">ID DEL TICKET</p>
                    <h4 class="fw-bold">#{{ entrada.entradaId }}</h4>
                </div>

                <hr>

                <div class="row mb-3">
                    <div class="col-6">
                        <span class="text-muted d-block small">Visitante</span>
                        <strong>{{ app.user.userIdentifier }}</strong>
                    </div>
                    <div class="col-6 text-end">
                        <span class="text-muted d-block small">Tipo de Ticket</span>
                        <strong>{{ entrada.tipoEntrada.tipo }}</strong> {# Asume que 'tipo' existe en TipoEntrada #}
                    </div>
                </div>

                <div class="row mb-3">
                    <div class="col-6">
                        <span class="text-muted d-block small">Fecha de Compra</span>
                        <span>{{ entrada.fechaCompra|date('d/m/Y H:i') }}</span>
                    </div>
                    <div class="col-6 text-end">
                        <span class="text-muted d-block small">Fecha de Visita</span>
                        <span class="badge bg-primary fs-6">{{ entrada.fechaVisita|date('d/m/Y') }}</span>
                    </div>
                </div>

                <hr>

                <div class="alert alert-info small mt-3">
                    <strong>Nota:</strong> Por favor, presente este recibo digital o una versión impresa en la entrada de la Puerta Sur.
                </div>
            </div>

            <div class="card-footer bg-light p-3 text-center">
                <a href="{{ path('app_home') }}" class="btn btn-outline-secondary btn-sm">Volver al Inicio</a>
                <button onclick="window.print()" class="btn btn-primary btn-sm ms-2">Imprimir Ticket</button>
            </div>
        </div>
    </div>
{% endblock %}
